/*
 * Comments on multiple lines.
 * { ( [ <- Should not raise an error.
 * // <- Should not cause the program to act weird.
 */

// " <- Is not an unclosed quote

#include <stdio.h>

int main(void) {
    // Print "Hello, World!" to the standard output
    printf("Hello, World!");
    putchar('\n');
    return 0;
}
